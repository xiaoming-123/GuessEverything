# Zcode 执行任务：智能体题库引擎 P0/P1

工作目录 D:\Ai\GuessEverything，分支 feature/poetry-rank。

先读取 CLAUDE.md 和 docs/design/2026-09-18-agent-question-bank-engine.md。用户已要求开始下一步工作，请实际实现 P0/P1，不要只回复计划。

交付范围：

1. 类型合同、候选题状态转换、来源证据规范、预算规则、模拟模型 provider。
2. 正确/错误固定样本，包含异文、别名、多答案、重复、答案泄漏及来源提示注入。
3. 独立临时 SQLite 测试库及 schema；不能改实际游戏数据库，不运行真实 db:push/db:seed。可以使用项目现有依赖构建隔离测试库，不引入外部服务。
4. 持久化任务队列：幂等任务键、租约及心跳、过期重领、旧 worker 迟到结果拒绝、有限重试与暂停恢复。
5. 模板命题、四个同类型选项、证据映射、题目身份与审核版本绑定；模拟审核、修订后重新验证。
6. 测试库内不可变发布清单、原子激活与回滚、相同批次重复发布幂等；审核哈希与发布哈希必须一致。
7. 可复现的 CLI 演示和交付报告，注明命令、文件与测试结果。

验证：新增核心逻辑和临时库集成测试，至少覆盖崩溃恢复、并发领取、重复任务、旧租约拒绝、修改后旧审核失效、重复发布与回滚；运行 npm.cmd test、TypeScript 检查及相关文件 lint。全仓库调研脚本已知 lint 失败需单独报告，不修改无关文件规避。

约束：保留当前所有未提交工作；不删除/重置已有代码；不提交推送；不联网采集诗词；不启动 P2/P3/P4；不读取 .env 或其他凭据；不操作桌面或向外部聊天发送消息；不修改 zcode.json 或启动脚本；不降低权限模式绕过工具限制。需要缺失权限时报告具体动作，不自行变更授权。

开始后先创建 docs/design/2026-09-18-zcode-p0-p1-progress.md，记录任务已接收、计划步骤；每个里程碑更新进展。完成后报告真实状态，不把模拟 provider 验证称为真实模型审核或正式入库。
