# 阶段 6 收官方案：封面后期 + 首页接入 + 终验（2026-09-21）

> 依据：优化方案 §8 实施顺序阶段 6「封面后期（叠真实标题）+ 全部资产入库前端 + 终验」。
> 现状盘点：P2 三期（立绘/限时事件/分享卡）已合 master（1e262a1）；`cover_v1.png` 为干净底图
> （VISION 确认：无叠加文字、上方 ~55% 纯天空留白、中央金袍人物、冷调水墨 + 琥珀金焦点）；
> `art-assets.ts` 已导出 `COVER_ART` 但**首页 `src/app/page.tsx` 未使用**（仍是纯文字 header）。
> 本方案做完后，既定实施顺序（阶段 1-6）全部闭环；外观系统 / 赛季重置属 P3，另行设计不在本文。

## 红线继承

- 答案永不下发；纯逻辑层零 IO；美术引用只走 `art-assets.ts` 常量表；语料不动。
- 封面标题用**真实字体渲染**（PIL + 系统字体），不走 AI 生图——避免伪汉字风险（美术纪律：AI 出图不可控文字）。

## 1. 封面后期（叠真实标题）

- 工具：`D:/soft/conda/python.exe` + Pillow（已确认 11.3.0 可用）；字体 `C:/Windows/Fonts/simkai.ttf`（楷体，国风契合）。
- 产物：`public/art/cover_v2.png`（1024×1024，v1 底图 + 标题层），脚本存 `scripts/cover-title.py`（幂等可重跑，seed 台账口径外的后期加工）。
- 版式（VISION 结论：上方天空区叠字，避开中央人物与左右建筑）：
  - 主标题「谜盒」：画面上部居中，~150px 楷体，琥珀金（#F5B841 系）+ 深靛蓝描边/阴影（与游戏内 indigo/amber 主题一致）；
  - 副标题「诗词升官」：主标题下方，~56px，暖白/淡金；
  - 底部白雾区小字「架空称号 · 非真实官制」（合规声明，~28px，灰蓝低对比）。
- `art-assets.ts`：`COVER_ART` 指向 `/art/cover_v2.png`（只改常量表，组件不动——D5 纪律）。

## 2. 首页接入（全部资产入库前端的最后一块）

- `src/app/page.tsx` header 改为封面卡：`COVER_ART` 经 art-assets 导入，`<img>` 圆角卡 + 标题叠在图上（图自带标题，页面不再重复大标题）；
  副行保留一句玩法引导；「诗词升官」模式卡不动。
- 首页为纯静态壳（无 API），不触碰加密层。

## 3. 终验（本玩法主线全量回归）

- 单测：art-assets 形状断言扩展（COVER_ART 以 `/art/` 开头）；全量 `npm test` 应 303+ 全绿。
- `tsc --noEmit` + eslint 改动文件 + `npm run build`。
- 浏览器（playwright-core 验收脚本，既有绕过方案）：
  1. 首页封面 img 解码（naturalWidth ≥ 1000）+ 截图 VISION 目视（标题无伪字、构图不压人物）；
  2. `/play/poetry-rank` 冒烟：主页渲染、研习开局、事件横幅/立绘挂载点回归（P2 三期不回退）；
  3. 截图存证。
- 验收纪律：全过后一个 commit，首行引用本方案。

## 明确不做

- 外观/皮肤系统、赛季重置与榜单定格（P3 另行设计）；
- 封面不重生成（v1 底图定稿，仅后期叠字）；
- 不动玩法逻辑 / 语料 / 加密层。
